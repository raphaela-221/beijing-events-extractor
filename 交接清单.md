# 前后端改造 · 交接清单

> 写给接手实施的下一个 agent（或几个月后回来的自己）。目的：不用重新翻聊天记录就能直接动手。
>
> 现状：**设计与规划阶段已完成，代码尚未开始写**。以下是读的顺序、已经拍板的决定、以及从哪里开始动手。

---

## 1. 按这个顺序读

| 顺序 | 文件 | 干什么用 |
|---|---|---|
| 1 | `Web化评估与计划.md` | 部署架构、Windows 离线包、canonical 写回策略。先懂"为什么服务器没有 Node/没有外网"这个硬约束 |
| 2 | `Web前端设计需求.md` | 视觉与交互设计 brief，给设计模型看的，信息架构和线框思路 |
| 3 | **`Web前端开发指南.md`（主文档，先读这个）** | 技术栈、job registry 架构、运行记录子系统、删除策略、用户视角规范、P0–P3 分阶段步骤、完整 API 契约、组件清单 |
| 4 | `mockups/index.html` | 9 页高保真静态原型，双击打开可点击跳转，视觉效果的最终参照 |
| 5 | 本文件 | 已拍板决定 + 当前状态 + 下一步具体做什么 |

`使用说明.md` 是现有桌面流程的文档，Web 化后逐步退役，仍需读一遍搞懂脚本原本怎么用。

---

## 2. 已拍板，不要再问

`Web前端开发指南.md` 附录 E 原本有 6 个待确认项，现已全部裁决并写回文档正文，直接按文档执行：

1. **`02_calendar/overrides/` 移出范围**——仓库里从未创建过这个目录，说明没真正用过，不做管理界面
2. **`run_pipeline.py eventlist` 移出 registry**——服务器上无意义（本机打开 Excel），核对页下载按钮取代它
3. **账号体系：v1 做管理员 / 普通操作员两个角色**——登录接口要返回角色；前端按角色过滤 `admin_only` 的 registry 条目；**后端对应接口必须做角色校验，不能只靠前端隐藏**
4. **`admin_only` 范围**：`canonical_dedup_write`（尤其 `--prune-malformed`）、`canonical_export_raw`、`canonical/rollback` 三个操作限管理员
5. **保留策略默认值**：日志 180 天 / 2GB 上限 / 每操作最少保留 10 次 / 回收站 30 天 / `.bak` 20 份
6. **长任务完成提示音默认关**——多人共享环境，默认出声容易扰民，设置页可开

如果发现执行中这些决定有问题（比如账号体系做起来发现单账号更省事），**回来跟产品 owner 确认，不要自己改**——这些是她拍板过的，不是我猜的默认值。

---

## 3. 现在有什么、没有什么

**有**：
- 完整技术选型和理由（`Web前端开发指南.md` §1）
- 19 个操作的 job registry 全量清单和字段定义（§2）
- 运行记录的 SQLite 表结构、目录约定、生命周期状态机（§3）
- 删除/回收站/自动清理的完整交互规范（§4）
- 用户视角的关键设计原则：断线重连回放、dry-run 前置、已知错误文案、下一步建议算法（§5）
- P0–P3 分阶段步骤和每阶段验收清单（§6）
- 前端目录结构、REST/SSE 接口全清单、组件状态清单、配色复用来源（附录 A–D）
- **9 页高保真静态 HTML 原型**（`mockups/`），纯 CSS/HTML 无框架，可点击跳转，主色已对齐参考设计 pipelinepro（`--primary: #4F46E5`）

**没有**：
- 任何后端代码（FastAPI 项目还没建）
- 任何前端工程代码（Vite 项目还没建，`mockups/` 是静态原型不是产物）
- registry 的实际 Python 实现
- 数据库迁移脚本

---

## 4. 从哪开始动手

严格按 `Web前端开发指南.md` §6 的 P0 走，不要跳到 P1/P2：

1. 建 FastAPI 后端骨架：`runs` / `run_deletions` 表建表，`data/runs/` 目录写入，子进程 stdout 逐行落 `stdout.jsonl`
2. registry **只登记一条**：`step1_concert_only`（日志量最大，最能压出虚拟滚动和断点续传的问题）
3. 建前端骨架：`npm create vite@latest frontend -- --template react-ts`，装 antd/@tanstack/react-query/zustand/react-router-dom
4. 做 `JobForm` / `JobRunner` / `useJobLog` 三件套，参照 `mockups/任务监控.html` 的视觉和交互（阶段步骤器、日志面板配色、自动滚动+暂停逻辑、摘要卡）
5. **P0 验收的硬指标**（§6 原文）：关掉标签页跑到一半再打开，日志从第 1 行完整回放且能继续接收新日志。这条做不到，后面全部返工，不要跳过

mockups 里的视觉细节（颜色变量、间距、字号）都定义在 `mockups/shared.css`，真实 React 项目里做成 AntD 的 theme token 时可以直接照抄这些数值，不用重新设计。

---

## 5. 如果卡住了

- 视觉细节和 mockups 不一致 → 以 mockups 为准，指南文字描述优先级低于原型图
- 指南和设计需求文档冲突 → 部署相关信息以《Web化评估与计划.md》为准，视觉相关以《Web前端设计需求.md》为准，实现顺序和接口契约以《Web前端开发指南.md》为准（这条写在开发指南开头）
- 遇到本清单和开发指南都没覆盖的产品判断 → 停下来问产品 owner，不要自己猜
