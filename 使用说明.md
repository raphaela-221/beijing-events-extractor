# 北京大事件整理project · 使用说明

> 一句话：把原始监测文件整理成**人工审核后的事件清单 Excel**，再自动生成可分享的**北京大事件日历网页**（总览 / 月历 / 时间轴）。
>
> 适用：社交媒体监测 / RM 团队，按月梳理北京文旅、赛会、政务、公共安全等城市脉搏事件。

---

## 1. 30 秒上手

### Windows（双击）

1. 双击项目根目录的 `setup.bat`，一次性装好依赖。
2. 把原始文件放进 `01_raw_input/`。
3. 双击 `run.bat` → 选 `[1] 构建事件清单`。
4. 在 `01_event_list_output/Events List.xlsx` 里人工核对、编辑、保存。
5. 再次双击 `run.bat` → 选 `[2] 生成日历看板`。
6. 把 `02_web_output/` 文件夹上传到 OneDrive，**分享文件夹链接**。

### macOS（终端）

```bash
cd /Users/raphaela-221/ClaudeCode/北京大事件整理project
chmod +x setup.sh run.sh
./setup.sh            # 只需一次
./run.sh              # 按菜单选择 1 或 2
```

> 如果双击 `run.bat` 一闪就关，或想精确排查，切换到下方第 5 节的“终端兜底”。

---

## 2. 完整工作流

```
原始监测文件
    ↓  Step 1
01_event_list_output/Travel_Facilitators_and_Hindrances_Events_YYYY.MM.DD.xlsx
    ↓  人工核对、去重、补充
01_event_list_output/Events List.xlsx   ← 唯一人工审核 canonical 文件
    ↓  Step 2
02_web_output/                          ← 可上传 OneDrive 的静态网页包
```

核心原则：

- `01_event_list_output/Events List.xlsx` 是 Step 2 的**唯一数据源**，也是最终网页里下载按钮提供的 Excel。
- Step 2 只增量更新变化的月份主题，不会每次全量调用 Ark。
- HTML/JS/CSS 可手动微调，修改源文件或放入 `02_calendar/overrides/` 后，下次打包仍然保留。

---

## 3. 文件 / 文件夹清单

| 路径 | 作用 |
|------|------|
| `01_raw_input/` | Step 1 输入：原始监测文件（xlsx/xls/csv/txt/json/doc/docx/pdf） |
| `01_event_list_output/` | Step 1 输出：增量合并后的 dated Excel + **人工审核 canonical 文件** `Events List.xlsx` |
| `01_event_extractor/` | Step 1 代码：`main.py`、`src/`、演唱会缓存 `state/` |
| `02_calendar/` | Step 2 源码和工作区：HTML/JS/CSS、Python 脚本、增量状态 `state/last_run.json` |
| `02_calendar/overrides/` | 可选：放手动微调后的 HTML/JS/CSS，打包时会覆盖到 `02_web_output/` |
| `02_web_output/` | Step 2 输出：可上传 OneDrive 的静态网页分享包 |
| `setup.bat` / `setup.sh` | 一次性安装依赖（含 Playwright Chromium） |
| `run.bat` / `run.sh` | 顶层菜单：选 Step 1 / Step 2 |
| `requirements.txt` | 两步共用 Python 依赖 |
| `使用说明.md` | 本文档（总流程唯一入口） |
| `01_raw_input/README.md` | Step 1 输入文件命名规则 |

---

## 4. 输入数据要求

### Step 1 输入

- 把原始监测文件放进 `01_raw_input/`。
- 支持格式：xlsx / xls / csv / txt / json / doc / docx / pdf。
- 命名规则见 `01_raw_input/README.md`。

### Step 2 源 Excel 要求

- 必须是 `01_event_list_output/Events List.xlsx`（canonical 文件）。
- 必须包含一个名为 **`Events List`** 的工作表。
- 必须包含以下列（列名完全一致）：

| 列名 | 说明 |
|------|------|
| `No.` | 序号 |
| `Topic` | 中文名在前，换行后接英文名，如 `文娱活动\nEntertainment` |
| `Link` | 来源链接 |
| `Start Date` | 开始日期（Excel 日期格式） |
| `End Date` | 结束日期（Excel 日期格式） |
| `Priority` | 只保留 `High` 的事件进入 calendar |
| `Event Keywords` | 事件关键词 |
| `Event English Keywords` | 英文关键词 |
| `Event Description` | 事件描述 |
| `Headline` | 事件标题 |
| `备注` | 可选，补充说明 |

---

## 5. 怎么运行

### 5.1 推荐方式：顶层菜单

| 平台 | 命令 / 操作 |
|------|-------------|
| Windows | 双击 `run.bat`，按 `1` 或 `2` |
| macOS | `./run.sh`，按 `1` 或 `2` |

### 5.2 终端兜底（bat 或菜单出问题用）

**Step 1：原始数据 → 增量事件清单**

```bash
# macOS
python3 01_event_extractor/main.py --concert

# Windows
python 01_event_extractor\main.py --concert
```

常用参数：

- `--concert-force`：强制重抓演唱会。
- `--only-input`：只处理本地文件，不抓演唱会。
- `--only-concert`：只抓演唱会，不处理本地文件。
- `--input-dir` / `--output-dir`：改输入 / 输出目录。

**Step 2：事件清单 → 日历网页**

```bash
# macOS
cd 02_calendar
python3 run_pipeline.py extract
python3 run_pipeline.py themes --year 2026
python3 run_pipeline.py calendar
python3 run_pipeline.py package

# Windows
cd 02_calendar
python run_pipeline.py extract
python run_pipeline.py themes --year 2026
python run_pipeline.py calendar
python run_pipeline.py package
```

### 5.3 Step 2 单步命令速查

```bash
python3 run_pipeline.py extract              # 从 canonical Excel 抽取 High 事件
python3 run_pipeline.py eventlist            # 用系统默认程序打开 canonical Excel
python3 run_pipeline.py themes --year 2026        # 增量 Ark 主题润色（只跑变化月份）
python3 run_pipeline.py themes --month 2026-07    # 强制重新跑某一个月
python3 generate_monthly_themes.py --month 2026-07 --compare  # 同时对比 Ark 与 Qwen
python3 run_pipeline.py calendar                  # 校验页面文件是否齐全
python3 run_pipeline.py package              # 打包 02_web_output/ 文件夹
python3 run_pipeline.py all --year 2026 --themes --package  # 一键跑 extract + themes + package
```

---

## 6. 输出在哪、看什么

| 输出 | 位置 | 说明 |
|------|------|------|
| 增量事件清单 | `01_event_list_output/Travel_Facilitators_and_Hindrances_Events_YYYY.MM.DD.xlsx` | Step 1 产物，按日期命名 |
| 人工审核 canonical 清单 | `01_event_list_output/Events List.xlsx` | Step 2 唯一数据源，也是网页下载文件 |
| 事件数据 | `02_calendar/demo_data.js` / `demo_data.json` | 页面读取的数据，系统自动生成 |
| 增量状态 | `02_calendar/state/last_run.json` | 记录每行事件签名和变化月份 |
| 整合总览 | `02_calendar/index.html` | 信号卡片、本月观察、关注列表、分布 |
| 月历视图 | `02_calendar/month.html` | 整月每天的事件密度 |
| 时间轴视图 | `02_calendar/timeline.html` | 事件持续期和重叠 |
| 网页分享包 | `02_web_output/` | Step 2 产物，上传 OneDrive 即可 |
| 手动微调覆盖 | `02_calendar/overrides/` | 改过的 HTML/JS/CSS 放这里，打包时覆盖 |

---

## 7. 增量更新机制

Step 2 使用 `02_calendar/state/last_run.json` 记录每行事件的签名（基于 `Topic / Start Date / End Date / Headline / Keywords / Description / Link` 的 SHA256）。

- 每次 `extract` 会重新计算签名。
- 与上一次对比，得到**新增 / 删除 / 修改**的事件。
- 根据这些事件影响的月份，生成 `changed_months` 列表。
- `themes` 步骤只重新调用 Ark 生成 `changed_months` 里的月份；其他月份直接复用已有主题。

这意味着：

- 首次运行：所有有事件的月份都会跑 Ark。
- 后续只改了几条 6 月事件：只有 `2026-06` 会重新跑 Ark。
- 如果你希望强制重跑某个月，用 `python3 run_pipeline.py themes --month 2026-06`。

---

## 8. 手动微调 HTML / JS / CSS

生成页面后，如果细节不合适，**直接修改 `02_calendar/` 里的源文件即可**，例如：

- `02_calendar/index.html`
- `02_calendar/common.js`
- `02_calendar/calendar.css`

下次 `package` 会把这些文件复制到 `02_web_output/`，改动自然保留。

### overrides/ 是干什么用的？

`02_calendar/overrides/` 是**可选兜底目录**，适合“只想改 output，不想动源码”的场景。打包时，`overrides/` 里的文件会**额外覆盖**到 `02_web_output/`，覆盖顺序在源文件复制之后。

| 修改方式 | 推荐度 | 适合场景 |
|---|---|---|
| 直接改 `02_calendar/` 源文件 | ⭐ 日常使用 | 长期、通用的调整 |
| 放到 `02_calendar/overrides/` | 偶尔用 | 临时/客制化覆盖，或不想污染主源码 |

> 注意：如果 `overrides/` 里放了 `common.js`，主题更新不会自动同步进去，需要手动维护。

---

## 9. 打包分发给其他人

### 9.1 操作步骤

1. 跑完 Step 2，确认 `02_web_output/` 文件夹已生成（约 4 MB）。
2. 把 `02_web_output/` 整个文件夹上传到 OneDrive。
3. 右键 `02_web_output/` 文件夹 → **共享** → 生成“任何人可查看 / 下载”的链接。
4. 把链接发给对方。

> 必须分享**文件夹**链接。单独分享 `index.html` 文件链接会导致 `month.html` / `timeline.html` 跳转失效。

### 9.2 `02_web_output/` 里有什么

只包含对方查看页面所需的最小文件：

- `index.html` — 整合总览
- `month.html` — 月历视图
- `timeline.html` — 时间轴视图
- `common.js` / `calendar.css` / `demo_data.js` — 逻辑、样式、数据
- `Events List.xlsx` — 页面上的“事件清单（Excel）”下载按钮提供的文件
- `README.txt` — 给收件人看的简短说明

### 9.3 更新时的好处

- `package_output.py` 不会删除整个 `02_web_output/` 文件夹，只会覆盖有变化的 viewer 文件。
- 它**不再删除** output 文件夹里额外的文件，所以你手动添加的说明、logo 等会保留。
- OneDrive 的**分享链接保持不变**。
- 数据小改时，通常只有 `common.js` 和 `demo_data.js` 会变化，对方如果是 OneDrive 同步状态，几乎瞬间就能看到更新。

---

## 10. 常见问题

| 现象 | 原因 | 解决 |
|------|------|------|
| `run.bat` 一闪就关 | 没有先运行 `setup.bat`，或 Python 没加 PATH，或找不到 canonical Excel | 先运行 `setup.bat`；确认 `01_event_list_output/Events List.xlsx` 存在；或改用终端方式 |
| `ModuleNotFoundError: openpyxl` | 依赖没装 | 运行 `setup.bat` / `setup.sh` |
| Step 2 说找不到源 Excel | `01_event_list_output/Events List.xlsx` 不存在 | 先跑 Step 1，或在该目录创建 `Events List.xlsx` |
| 页面打开空白 / 事件不显示 | `demo_data.js` 没生成 | 在 `02_calendar/` 运行 `python3 run_pipeline.py extract` |
| 修改 Excel 后页面没更新 | 编辑后没重新跑 extract | 保存后重新运行 Step 2，或手动跑 `extract` + `package` |
| 事件清单里日期是错的 | 源 Excel `Start Date` / `End Date` 格式不标准 | 在 Excel 里把日期列设为“日期”格式，重新抽取 |
| 类型颜色 / 名称不对 | `Topic` 列格式不是“中文\nEnglish” | 检查 `Topic` 列是否有换行；确认 `common.js` 的 `TOPIC_COLORS` 包含该类型 |
| 主题句像编的 | 没跑 Ark 或 Ark 调用失败 | 在 `02_calendar/` 运行 `python3 run_pipeline.py themes --year 2026` |
| 手动改了 output 里的 HTML，下次打包被覆盖 | 打包脚本会覆盖 `02_web_output/` 里的生成文件 | 把改好的文件放到 `02_calendar/overrides/`，或改 `02_calendar/` 里的源文件 |
| 修改 Excel 后 Ark 还是跑了很多月 | `state/last_run.json` 可能被删除，或 Excel 里改动影响到了多个月 | 检查 `state/last_run.json` 中的 `changed_months` |
| 如何强制重跑某个月主题 | 需要覆盖已缓存的主题 | `python3 run_pipeline.py themes --month 2026-06` |

---

## 11. 已知限制

- Calendar 只展示 `Priority = High` 的事件；中低优先级事件仍在源 Excel 里，但网页不展示。
- 事件标题、描述、关键词保持中文原文；英文报告只做分类和总结翻译。
- “本月观察”如果未跑 Ark，则使用基于事件数量的规则模板，不会深入语义。
- Ark 主题生成需要联网访问火山引擎 endpoint；离线环境不可用。
- HTML 文件不能作为邮件附件直接发送，需通过 OneDrive / 内部静态站点分享。

---

## 12. 分发自检清单

- [ ] `02_web_output/` 已生成，大小约 4 MB。
- [ ] `02_web_output/Events List.xlsx` 与 `01_event_list_output/Events List.xlsx` 内容一致。
- [ ] `02_web_output/demo_data.js` 已重新生成。
- [ ] OneDrive 分享的是**文件夹**链接，不是单个文件链接。
- [ ] 分享后自己点开链接测试 `index.html` 能正常跳转月历 / 时间轴。

---

## 13. 版本与变更

| 日期 | 变更 |
|------|------|
| 2026-07-07 | 把流程串成 `run_pipeline.py`；新增 Qwen 主题生成；新增 `package_output.py` 打包 `02_web_output/` 用于 OneDrive 分享。 |
| 2026-07-08 | 重组文件夹：明确拆分为 Step 1（`01_event_extractor`）和 Step 2（`02_calendar`）；新增顶层 `setup.bat` / `setup.sh` / `run.bat` / `run.sh`。 |
| 2026-07-09 | Step 2 改为以 `01_event_list_output/Events List.xlsx` 为唯一人工审核数据源；打包时直接复制该文件到 `02_web_output/`；新增增量检测 `02_calendar/state/last_run.json`，主题生成只重新跑变化月份；新增 `02_calendar/overrides/` 机制保留手动 HTML/JS/CSS 微调；合并使用说明为单一文档。 |
| 2026-07-10 | 日历首屏改为基于当前日期动态初始化；Step 1 / Step 2 LLM 全部切换为火山引擎 Ark（deepseek-v4-flash）；主题生成新增 `--compare` 可自动生成 Ark 与 Qwen 对比文件。 |
