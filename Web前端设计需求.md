# 北京大事件整理系统 · Web 前端设计需求文档

> 本文档是给**前端设计模型/设计师**的 brief。目标：把现在「同事在自己电脑双击 `run.bat`」的桌面自动化流程，改造成一个部署在公司 Windows 服务器上、浏览器访问的**内部操作台**。请基于本文做初步设计（信息架构 + 低保真线框 + 关键交互流 + 视觉方向）。

---

## 0. 一句话背景

一个社交媒体监测团队每月要把原始监测文件整理成「北京城市大事件日历网页」。现在靠本地 Python 脚本 + `run.bat` 跑。要做的是一个**内部 Web 操作台**，让同事在浏览器里完成全流程：上传原始文件 → 自动抽取事件 → 人工核对 → 生成日历网页 → 拿到分享链接。

---

## 1. 产品目标与边界

### 1.1 目标
- **取代 `run.bat`**：同事不再需要在自己电脑装 Python、配 API key、双击 bat。打开内部网址就能跑全流程。
- **进度可见**：原来终端打印的进度日志（LLM 调用、演唱会采集计数、提取摘要）必须在网页上实时看到，不能黑盒转圈。
- **发布即更新**：日历网页已由项目管理平台托管对外分享（替换原文件、URL 不变）；操作台内置预览（发布前核对）+ 发布时生成 zip 供操作员手动上传到平台（v1）。v2 若平台支持上传 API 再接一键自动上传。

### 1.2 不做什么（明确排除）
- **不重新设计「读者端日历」**：日历网页（总览页 / 月历页 / 时间轴页）已经存在**并已部署在服务器上对外分享**，**不在本次设计范围**。操作台只需「预览」+「推送更新」+「复制链接」，不要 redesign 那三个页面。
- **不做网页内电子表格编辑**（v1）：人工核对采用「下载 Excel → 桌面改 → 上传回传」的往返方式。网页内直接编辑表格是 v2 的事，本次不设计。
- **不做多用户协作功能**（v1）：同一时间只允许一人操作核心环节（见下方「编辑锁」）。

### 1.3 受众
- **主要用户**：社交媒体监测 / PR 团队成员（非程序员），每月跑 1–2 次全流程。
- **使用频率**：低频（月度），单次会话可能 20–60 分钟（含等待 LLM 和采集）。
- **设备**：公司 Windows 电脑，浏览器访问。也有人在 Mac 上用。

---

## 2. 核心工作流（UI 必须完整支撑这条链）

```
登录
 └─ 总览仪表盘（看当前状态 + 入口）
     ├─ Step 1：上传原始文件 + 配置采集选项 → 点「开始抽取」
     │     └─ 任务监控（实时日志 + 进度 + 阶段标记）→ 完成摘要
     ├─ 人工核对：下载 canonical Excel → 桌面改 → 上传回传 → 校验 + 差异摘要
     ├─ Step 2：配置年份等选项 → 点「生成日历」
     │     └─ 任务监控（实时日志）→ 完成摘要
     └─ 日历预览与发布：预览 + 一键推送更新到已上线日历 + 复制分享链接
```

关键点：**Step 1 和 Step 2 都是耗时几分钟的后台任务**，UI 必须把「任务进行中」的状态和实时日志做好，这是体验成败的关键。

---

## 3. 硬约束（设计必须尊重）

| 约束 | 对设计的含义 |
|---|---|
| **单用户编辑锁** | 核心环节（核对/生成）同一时间只一人。当有人占用时，其他人要看到明确的「XX 正在编辑 / 占用中」提示，并禁用相关按钮。 |
| **核对 = 下载/上传往返** | 核对页不是在线表格编辑器，而是「下载当前 Excel → 本地改 → 拖拽上传回传 → 系统校验 + 显示差异」。要把这个往返流程设计得顺滑、不让人迷路。 |
| **长任务 + 实时日志** | Step1/Step2 跑几分钟，必须实时显示日志流（替代原来终端输出）+ 阶段进度，不能只有一个转圈 spinner。 |
| **上传回传要校验** | 上传的 Excel 会被自动校验完整性（损坏要拒绝并提示），并显示「新增/删除/修改」差异摘要。 |
| **中文为主** | 所有 UI 文案中文；事件数据（标题/描述/关键词）中文原文。英文只出现在分类标签的副标题和代码/技术词。 |
| **内部工具调性** | 不是面向公众的营销页，是数据密集的内部操作台。专业、克制、信息清晰优先，不要花哨。 |

---

## 4. 技术交互模型（设计师需了解，便于设计异步状态）

- 前端是 SPA（单页应用），通过 **REST API** 调后端，通过 **SSE（Server-Sent Events）** 接收实时日志流。
- 后端是 Python FastAPI，部署在 Windows 服务器上（走离线安装包，便携 Python，免管理员）。日历静态页**已由项目管理平台托管对外分享**；操作台另内置一份预览托管（FastAPI StaticFiles），并把更新以 zip 上传推送到平台。
- 任务是**后台异步**的：点「开始」→ 立即返回任务 ID → 前端订阅该任务的日志流 + 轮询状态。
- 因为单用户锁，**同一时间只有一个任务在跑**；新任务发起前要检查是否已有任务占用。

设计师需要为以下**任务状态**设计对应 UI：
`空闲` / `排队` / `运行中`（含当前阶段）/ `成功` / `失败` / `已取消`

---

## 5. 页面清单与每页职责

### 页面 1：登录
- 极简：用户名 + 密码 + 登录按钮。
- 内部工具，不需要注册/找回密码的复杂流程（可放一个「联系管理员」链接）。

### 页面 2：总览仪表盘（首页）
- **定位**：进入后的指挥中心，一眼看到「现在是什么状态 + 我下一步该干嘛」。
- **展示**：
  - 当前 canonical 事件清单：总事件数、High 优先级数、覆盖月份范围、最后修改时间。
  - 最近一次发布：时间 + 分享链接 + 「复制链接」按钮。
  - 任务历史：最近 3–5 次任务（Step1/Step2 各一行，状态 + 时间 + 用时）。
  - 系统占用状态：当前是否有人在编辑/有任务在跑（编辑锁状态）。
- **快捷入口**：4 个大按钮卡片对应流程四步（上传抽取 / 人工核对 / 生成日历 / 预览发布），按流程顺序排列，已完成的步骤打勾。

### 页面 3：Step 1 上传与抽取
- **上传区**：拖拽上传多个原始监测文件（支持 xlsx/csv/txt/json/doc/docx/pdf）。显示已添加文件列表（文件名 + 大小 + 可移除）。
- **采集选项**（折叠区，给高级用户）：
  - 是否同时采集演唱会（开关，默认开）。
  - 演唱会采集：强制重抓（开关）、日期范围、最小场馆规模（small/medium/large 下拉）。
  - 提取模式：auto / 春假 / 事件总结（默认 auto）。
  - 可选：手动指定分类、补充说明文本框。
- **操作**：「开始抽取」主按钮。点击后若检测到已有任务在跑或编辑锁占用，先提示。
- **提交后**：跳转到「任务监控」（或在本页展开监控面板）。

### 页面 4：任务监控（关键页，可做成面板或独立页）
- **这是体验的核心**。原来 bat 在终端打印的所有信息都要在这里实时呈现。
- **展示**：
  - 顶部：任务类型（Step1/Step2）+ 状态徽章 + 用时计时器 + 「取消」按钮（运行中时）。
  - 阶段进度条/步骤器：Step1 的阶段如「读取文件 → LLM 抽取 → 演唱会采集 → 合并去重 → 生成 Excel」；Step2 如「抽取 High 事件 → 生成月度主题 → 校验页面 → 打包」。当前阶段高亮，已完成打勾。
  - **实时日志区**：滚动的文本流，等宽字体，按级别配色（info 灰 / warn 橙 / error 红 / success 绿），自动滚到底部（可手动锁定滚动）。
  - **关键摘要卡片**（任务进行中/结束后高亮显示）：
    - LLM 用量：哪个 API（DeepSeek/Ark）+ token 消耗（脚本已有 `print_usage_summary`）。
    - 演唱会采集回显：列表页/详情页/过滤各阶段计数 + 失败样本（脚本已有 `[Concert]` 回显块）。
    - 提取摘要：事件总数、High 数、分类分布、重要事件 Top5。
  - 结束态：成功 → 「前往核对」（Step1 后）或「前往预览发布」（Step2 后）；失败 → 错误摘要 + 「重试」/「查看完整日志」。

### 页面 5：人工核对（下载/上传往返）
- **顶部**：编辑锁状态条（谁在编辑、开始时间；若被别人占用则提示并禁用操作）。
- **当前清单预览**：只读表格展示 canonical 事件（No. / Topic / Headline / 日期 / Priority / 关键词），支持排序、按 Priority/Topic 筛选、搜索。这是为了让同事下载前先看一眼、上传后核对差异。
- **下载**：「下载 Events List.xlsx」按钮（下载当前 canonical）。
- **上传回传**：拖拽上传修改后的 xlsx。
  - 上传后自动跑校验：损坏 → 红色提示拒绝；通过 → 显示**差异摘要**（新增 N 条 / 删除 N 条 / 修改 N 条，列出明细）。
  - 「确认覆盖」按钮才会真正写回 canonical；「取消」放弃。
- **辅助**：断点演唱会日期（Dates 列）的格式说明链接（指向帮助文档），因为这是手工补的、格式特殊。

### 页面 6：Step 2 生成日历
- **选项**：
  - 年份（默认当前年）。
  - 是否重新生成月度主题（默认增量；可选强制重跑某个月，下拉选月）。
  - 是否打包发布（默认开）。
- **操作**：「开始生成」→ 跳转任务监控。
- 完成后引导到「预览与发布」。

### 页面 7：日历预览与发布
- **两个 URL**：
  - 内置预览 URL（操作台自己托管，Step 2 跑完即更新，用于发布前核对）。
  - 对外分享 URL（项目管理平台上的日历）。两个都有「复制链接」+「在浏览器打开」。
- **预览**：三个 tab 预览「总览页 / 月历页 / 时间轴页」（iframe 嵌入内置预览，不要 redesign 这三个页面本身）。
- **发布到对外日历**：「发布更新」按钮（v1 手动）：生成 zip + 下载链接 + 「打开平台上传页」按钮 + 文字步骤；操作员手动上传后点「标记已发布」。成功提示对外 URL（固定不变）。
  - v2 若平台支持上传 API：升级为一键 zip + 上传 + 进度 + 自动回显。
- **发布历史**：最近几次发布时间 + 方式（自动 / 手动）。

### 页面 8：设置 / 历史（可选，v1 可极简）
- 任务历史列表（时间 / 类型 / 状态 / 用时 / 日志可展开）。
- LLM 用量统计（按月）。
- 管理员区：API key 配置状态（只显示「已配置/未配置」，不显示明文）、备份下载。

---

## 6. 关键交互模式（请重点设计）

### 6.1 长任务的发起 → 监控 → 收尾
- 发起按钮点击后立即禁用 + 文案变「正在启动…」→ 跳监控。
- 监控页的日志流要让人**确信系统在干活**，不是卡死：阶段步骤器 + 滚动日志 + 用时计时。
- 结束态要有明确的下一步引导（不要让人停在空白成功页）。

### 6.2 编辑锁的可视化
- 全局顶部一条窄状态条：空闲时灰（「当前无人占用」），占用时蓝（「XX 正在编辑，HH:MM 起」）。
- 被占用时，相关按钮置灰 + tooltip 说明。

### 6.3 下载/上传往返的引导
- 用一个清晰的「三步」视觉（下载 → 本地修改 → 上传回传），让同事不会忘了上传或下了旧版本。
- 上传后**必须**给差异摘要，让人放心覆盖。
- 「确认覆盖」要做二次确认（这是破坏性操作）。

### 6.4 文件上传
- 拖拽 + 点击两种方式。
- 格式/大小校验，非法文件即时红色提示。
- 上传进度条。

### 6.5 错误处理
- 失败任务不要只给「出错了」。要给：错误摘要（一句大白话）+ 可能原因 + 「查看完整日志」+ 「重试」。
- 常见错误要有针对性文案（如「canonical Excel 损坏」「演唱会采集失败：政府站点不可达，请检查网络」「API key 未配置」）。

---

## 7. 数据与字段（设计表格/卡片时参考）

canonical 事件清单字段：

| 字段 | 说明 |
|---|---|
| No. | 序号 |
| Topic | 分类，中文\n英文（如「文娱活动\nEntertainment」），有颜色映射 |
| Link | 来源链接 |
| Start Date / End Date | 日期 |
| Priority | High / 中 / 低；只有 High 进日历 |
| Event Keywords / Event English Keywords | 关键词 |
| Event Description | 描述 |
| Headline | 标题 |
| 备注 | 补充 |
| Dates | 演唱会离散场次（R 列，特殊格式如 `8.14-16, 8.19`） |

任务状态字段：类型、状态、当前阶段、日志行、开始/结束时间、LLM 用量、采集统计。

---

## 8. 视觉方向

- **调性**：现代后台操作台（参考 Linear / Vercel Dashboard 的克制感），数据密集但留白合理。专业、稳定、不花哨。
- **配色**：
  - 主色建议冷静的蓝或靛（信任、专业）。
  - 语义色：运行中=蓝、成功=绿、警告=橙、错误=红、空闲/禁用=灰。全站统一。
  - Topic 分类色可复用读者端日历已有的颜色映射（保持品牌一致）。
- **字体**：中文用系统无衬线（微软雅黑 / 苹方），等宽字体用于日志区（JetBrains Mono / Consolas）。
- **布局**：左侧固定主导航（仪表盘 / Step1 / 核对 / Step2 / 预览发布 / 历史），顶部全局状态条（编辑锁 + 当前任务），右侧主内容区。
- **密度**：表格紧凑可扫，但关键操作按钮要大、醒目。
- **响应式**：主要面向桌面（1280px+），不需要重点做移动端，但不要在窄屏下彻底崩。

---

## 9. 交付物要求（请设计模型产出）

1. **信息架构图**：页面清单 + 导航关系 + 主要跳转流。
2. **低保真线框图**：上述 8 个页面每页一张，标注关键元素和状态。
3. **关键交互流图**：至少覆盖 ①长任务发起-监控-收尾 ②下载/上传核对往返（含差异确认） ③编辑锁占用与释放。
4. **组件清单**：复用组件（状态徽章、阶段步骤器、日志流面板、文件上传区、差异摘要卡、任务历史行等）的列表与状态。
5. **视觉方向稿**：1–2 个核心页（建议「任务监控」+「人工核对」）的高保真视觉，含配色/字体/间距规范。
6. **设计说明**：关键设计决策的理由，以及你认为有风险/需要产品确认的点。

---

## 10. 补充说明给设计模型

- 你不需要懂 Python 或后端实现，只需按本文描述的状态和交互设计。
- 如果某个状态或流程在本文里没写清楚，**按最合理的假设设计并标注「假设：…，待确认」**，不要停下来等。
- 读者端日历（总览/月历/时间轴）**已存在**，不要 redesign；操作台里只是「预览 + 链接」。
- 优先把「任务监控页」和「人工核对页」设计好，这两个是体验成败的关键，其余页面可以相对常规。

---

## 附：流程现状（供理解背景，不需设计）

- **Step 1**：读 `01_raw_input/` 下原始文件 +（可选）从文化和旅游部政务服务平台采集北京演唱会 → LLM（DeepSeek 主、Ark 兜底）抽取结构化事件 → 增量合并去重 → 输出 Excel。
- **人工核对**：canonical 文件 `01_event_list_output/Events List.xlsx` 是唯一人工审核源，含 Excel 手工特性（表格/数据验证），同事在桌面 Excel 改。
- **Step 2**：从 canonical 抽取 High 事件 → 增量生成月度主题（Ark）→ 校验 → 打包成静态网页。
- **发布**：3 个日历 HTML 由项目管理平台托管对外分享（zip 上传）；Web 化后操作台内置预览 + 把更新以 zip 推送到平台，对外 URL 不变。
