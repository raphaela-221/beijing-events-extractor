# ============================================================
# LLM 配置
# ============================================================

# 主：DeepSeek 直连（OpenAI 兼容协议）
# 变量名是 OPENAI_*，但填的是 DeepSeek 的 key 和 endpoint。
# 模型用 deepseek-v4-flash（DeepSeek 直连支持）。
OPENAI_API_KEY=your-deepseek-api-key-here
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-v4-flash

# 兜底：火山引擎 Agent Plan（Anthropic 兼容端点）
# 注意 base_url 必须是 api/plan，不是 api/v3：
#   - api/plan（Agent Plan 套餐，给 Claude Code 用的那个）：Anthropic 协议，接受模型名 deepseek-v4-flash
#   - api/v3（普通推理）：OpenAI 协议，要 endpoint ID（ep-xxx），填模型名会 404
# 代码检测到 ARK_BASE_URL 含 /api/plan 时自动走 Anthropic 协议。
# DeepSeek 主调用失败（超时 / 限流 / key 失效）时自动切到 Ark。
ARK_API_KEY=your-ark-agent-plan-key-here
ARK_BASE_URL=https://ark.cn-beijing.volces.com/api/plan
ARK_MODEL=deepseek-v4-flash
