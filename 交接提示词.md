# 交接提示词

> 开新会话/新 agent 接手这个项目的前后端改造时，把下面这段话原样粘贴给它作为第一条消息。

---

我要把「北京大事件整理project」从桌面脚本流程改造成 Web 操作台（前端 + 后端）。设计和规划阶段已经完成，代码还没开始写。你不是从零调研，是接手一份已经拍板的方案，请按下面的顺序和思路推进，不要重新发明。

**第一步，按顺序读完这几份文档，读完之前不要开始写代码或问我需求：**

1. `Web化评估与计划.md` —— 部署架构和硬约束（服务器无 Node、无外网、免管理员的 Windows 离线包）
2. `Web前端设计需求.md` —— 视觉与交互设计 brief
3. `Web前端开发指南.md` —— **主文档**，技术栈、job registry 架构、运行记录子系统、删除策略、用户视角规范、P0–P3 分阶段步骤、完整 API 契约
4. `mockups/index.html`（双击打开，可点击跳转）—— 9 页高保真静态原型，视觉效果以这个为准
5. `交接清单.md` —— 已拍板决定的清单 + 现在有什么没什么 + 从哪一步具体开始动手

**读完之后：**

- `交接清单.md` §2 列的几项是产品 owner 已经拍板的决定（账号角色、`admin_only` 范围、保留策略默认值等），直接执行，不要再问我确认
- 严格按 `Web前端开发指南.md` §6 的 P0 → P1 → P2 → P3 顺序推进，不要因为「看起来简单」就跳到后面阶段。P0 只做一件事：把 `step1_concert_only` 这一个操作从发起到落盘到断线重连全部跑通，这是后面所有阶段的地基
- 每个阶段完成后，对照该阶段自己的验收清单（§6 里每个 P 都有），全部打勾才算完成，不要口头说"应该没问题"
- 视觉细节以 `mockups/` 里的静态原型和 `mockups/shared.css` 的变量值为准，指南里的文字描述如果和原型有出入，以原型为准
- 遇到指南和交接清单都没覆盖、需要产品判断的问题，停下来问我，不要自己猜；纯技术实现细节可以自己决定
- 这是一次比较大的改造，动手前先用 Plan Mode 把你打算怎么做 P0 说一遍给我确认，我看完再让你开始写
